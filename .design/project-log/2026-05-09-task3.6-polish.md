# Project Log - 2026-05-09 - Task 3.6 Polish

## Task Description
Address Phase 3 Review Feedback & Final Polish for the "grove" to "project" rename.

## Work Completed
1.  **Telemetry Improvements**:
    *   Updated `pkg/fabrictool/telemetry/gcp_exporter.go` and `pkg/fabrictool/telemetry/providers.go` to emit both `grove_id` and `project_id` attributes.
    *   Added fallback logic so that if only one of `FABRIC_GROVE_ID` or `FABRIC_PROJECT_ID` is present, both attributes are populated with that value during the transition period.

2.  **Cleanup & Alignment**:
    *   Updated residual 'legacy' comments in `pkg/api/types.go` (e.g., `AgentInfo.Project`).
    *   Removed redundant `MarshalJSON` for `ResolvedSecret` in both `pkg/api/types.go` and `pkg/hubclient/types.go`. `UnmarshalJSON` was retained for normalization.
    *   Updated `Project` struct in `pkg/hubclient/types.go` to use `projectType` tag and added custom `MarshalJSON`/`UnmarshalJSON` for dual-field support with `groveType`, `groveId`, and `groveName`.

3.  **Unit Testing**:
    *   Added comprehensive JSON marshaling/unmarshaling tests in `pkg/api/types_test.go`, `pkg/hubclient/types_test.go`, and `pkg/runtimebroker/types_test.go`.
    *   Verified:
        *   Legacy payload handling (unmarshaling `grove` fields into `project` fields).
        *   Field priority (new `project` fields win over legacy `grove` fields if both are present).
        *   Dual-field emission (marshaling into both `project` and `grove` keys).

4.  **Refactor Completion**:
    *   Committed uncommitted Phase 3 refactors in `pkg/hubclient` that implemented dual-field support and updated API paths from `/groves/` to `/projects/`.

## Verification Results
*   Ran `go test ./pkg/api/... ./pkg/hubclient/... ./pkg/runtimebroker/...`
*   All tests passed, including new dual-field logic tests.
*   Verified telemetry dual-emission logic via manual code review of the applied changes.

## Observations
*   The `pkg/hubclient` package had many uncommitted changes that were consistent with the Phase 3 goal and the review feedback. These were included in the final commit to ensure a consistent state.
*   `ResolvedSecret` marshaling was indeed redundant as the `source` field already existed; normalization is correctly handled in `UnmarshalJSON`.
