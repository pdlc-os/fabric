# Project Log: Address Code Review Issues (Grove to Project Rename)

**Date:** 2026-05-11
**Task:** Fix 4 issues identified in code review on `fabric/rename-strategy` branch.

## Work Completed

### 1. Fixed API Routing Copy-Paste Bug
- Updated `pkg/hub/handlers.go` in `handleProjectRoutes` and `handleProjectByID` to correctly use `/api/v1/groves/` and `/api/v1/groves` respectively in the `else` branches for backward compatibility.

### 2. Dual-Publish Events for Backward Compatibility
- Updated `pkg/hub/events.go` to publish to both `project.*` and `grove.*` subjects for all project-related events:
    - `PublishAgentStatus`
    - `PublishAgentCreated`
    - `PublishAgentDeleted`
    - `PublishProjectCreated`
    - `PublishProjectUpdated`
    - `PublishProjectDeleted`
    - `PublishBrokerConnected`
    - `PublishBrokerDisconnected`
    - `PublishUserMessage`

### 3. Updated CLI User-Facing Strings
- Updated `cmd/attach.go`, `cmd/clean.go`, `cmd/common.go`, and `cmd/server_dispatcher.go` to use "project" instead of "grove" in user-visible messages and logs.

### 4. Renamed `configCdGroveCmd`
- Renamed `configCdGroveCmd` to `configCdProjectCmd` in `cmd/config.go`.
- Updated `Use`, `Short`, and `Long` fields.
- Updated related helper functions and registration in `init()`.

## Verification
- Ran `go build ./...` to ensure project compiles correctly.

## Observations
- Many 'grove' references remain in internal variable names and internal comments, but user-facing strings were the priority for this task.
- Backward compatibility is maintained at the API and event level by dual-handling 'grove' and 'project' prefixes.
